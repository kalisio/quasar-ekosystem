import { describe, it, expect } from 'vitest'
import * as quasarMoment from '../src/index.js'

describe('quasar-moment', () => {
  it('exports a module', () => {
    expect(quasarMoment).toBeDefined()
  })
})
